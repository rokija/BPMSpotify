import React from 'react';

const BeforeTrackList = () => {
    return (
        <div className="before-tracklist-wrapper">
            <div className="arrow-up glyphicon glyphicon-menu-up" />
            <span className="before-tracklist">Enter text in Search bar to get track details</span>
        </div>
    );
};

export default BeforeTrackList;
