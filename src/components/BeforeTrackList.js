import React, { Component } from 'react';

export default class BeforeTrackList extends Component {
    render() {
        return (
            <div className="before-tracklist-wrapper">
                <span className="before-tracklist">Enter text in Search bar to ....</span>
            </div>
        )
    }
}